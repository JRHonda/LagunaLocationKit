import XCTest
@testable import LagunaLocationKit

final class LagunaLocationKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LagunaLocationKit().text, "Hello, World!")
    }
}
